<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/drawer_gallery"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Consultar armario" />

        <item
            android:id="@+id/add_new_garment"
            android:icon="@drawable/ic_menu_camera"
            android:title="Añadir nueva prenda" />

        <item
            android:id="@+id/add_new_look"
            android:icon="@drawable/icon_hanger"
            android:title="Crear un nuevo look" />

        <item
            android:id="@+id/drawer_calendar"
            android:icon="@drawable/ic_menu_calendar"
            android:title="Consultar calendario" />

    </group>

    <item android:title="Account">
        <menu>
            <item
                android:id="@+id/nav_disconnect"
                android:icon="@drawable/ic_menu_disconnect"
                android:title="Desconectar" />
        </menu>
    </item>

</menu>